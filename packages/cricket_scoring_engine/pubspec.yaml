name: cricket_scoring_engine
description: Pure Dart cricket scoring calculation engine and state machine for offline-first scoring.
version: 1.0.0
homepage: https://github.com/cricket/cricket_scoring_engine

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  meta: ^1.9.1

dev_dependencies:
  test: ^1.24.0
  lints: ^2.1.0
